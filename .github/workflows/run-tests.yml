name: cmus-status-scrobbler tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  Run-Tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 
      - run: echo "Starting tests..."
      - run: python tests.py
      - run: echo "Tests did not fail."
      
